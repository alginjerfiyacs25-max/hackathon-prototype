# AquaSentinel Backend

FastAPI simulation API for the AquaSentinel hackathon prototype.

## Run
From `backend`, install `requirements.txt`, then run `uvicorn app.main:app --reload`.
API docs are available at `http://localhost:8000/docs`.

All values and model metrics are simulated and are not real-world safety guarantees.
